<% post.comments.forEach(function(comment){ %>
    <p>
        <%= comment.name %>
        <span class="info">回复于<%= comment.time %></span>
    </p>
    <p><%- comment.content %></p>
<% }) %>
<% if(user) { %>
<div class="huifu">
    <form method="post" action="/comment/<%= post.name %>/<%= post.time.minute %>/<%= post.title %>">
        发布人:<%= user.name %>
        <input type="hidden" name="name" value="<%= user.name %>">
        <textarea name="content" rows="10" cols="80"></textarea><br>
        <input type="submit" value="提交" class="zhuce">
    </form>
</div>
<% }else{ %>
<div class="huifu">
    <form method="post">
        回应请先<a href="/login">登录</a>或者<a href="/reg">注册</a>
        <textarea name="content" rows="10" cols="80" id="tb"></textarea>
        <input type="submit" value="提交" id="sb">
    </form>
</div>
<% } %>
<script>
    window.onload = function(){
        var button = document.querySelector('#sb');
        var textarea = document.querySelector('#tb');
        button.onclick = function(e){
            e.preventDefault();
            location.href = '/login';
        }
        textarea.onfocus = function(e){
            e.preventDefault();
            location.href = '/login';
        }
    }
</script>


